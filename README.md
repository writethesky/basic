# Basic GRPC Server
The server contains the following services
* token service
* user service

## Development of the command

### Generate the necessary files
`make generate`

### Run Server
`make run`
